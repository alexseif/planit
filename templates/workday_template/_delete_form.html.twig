<form method="post" action="{{ path('app_workday_template_delete', {'id': workday_template.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workday_template.id) }}">
    <button class="btn btn-danger">{{ 'actions.delete'|trans }}</button>
</form>
