<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">{{ 'plan_it'|trans }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="{{ 'actions.toggle_navigation'|trans }}">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            {{ knp_menu_render('main',  {'currentClass': 'active', 'template': '_menu_knp_bootstrap5.html.twig'}) }}
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="{{ 'actions.search'|trans }}" aria-label="{{ 'actions.search'|trans }}">
                <button class="btn btn-outline-success" type="submit">{{ 'actions.search'|trans }}</button>
            </form>
        </div>
    </div>
</nav>